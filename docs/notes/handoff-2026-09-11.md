# Handoff: 2026-09-11

The current branch contains a narrow follow-up to `2ad46f6f`. It removes the
broad qualifier-depth inference rule, keeps the `handling(...).by(...)` result
open until its selected member supplies the type, and avoids re-inferring
compiler-defined casts while checking dynamic type applications. Standard
collection element fallback is now checked before the general parent walk, so
deeply nested `List` casts stay fast. The changes are in
`crates/typer/src/check.rs`, `check_apply.rs`, `check_expr.rs`,
`check_infer.rs`, and `check_select.rs`.

Validation completed before this handoff:

- `cargo fmt --all --check` and `git diff --check` passed.
- 72 focused regression tests passed across `cats3`, `dependentadaptation`,
  `final1`, `lexicalcontext`, `mismatch3`, `mismatch8`, `mismatch10`,
  `setmap`, and `varassign`.
- The two dynamic explicit-type-argument tests passed.
- Corpus probes for `infer2-pos`, `t9485`, `t6663`, `contrib467`,
  `isApplicableSafe`, `t6499`, `viewtest1`, `virtpatmat_reach_const`,
  `erasure-nsquared`, and `Course-2002-05/08/09` passed. The deep-cast
  timeout reproduced on the unguarded implementation and passed after the
  guard.

The full merge gate has **not** been rerun after this follow-up. The previous
`2ad46f6f` gate was `VERDICT=FAIL`: it had four workspace failures and nine
corpus losses caused by the broad qualifier rule. Run one fresh gate on the
committed follow-up before updating `tests/BASELINE.md` or saving a new corpus
ledger. Use the accepted baseline `c0c10f08` named by that file, the checked
corpus at `/private/tmp/scala-rs-corpus-20260910-codex`, and a private
`GATE_DIR`; wait for the `DONE` marker and require both `VERDICT=PASS` and
`losses=0`.

The repository's permanent rules remain in `.agent-brief.md`. Preserve the
clean working tree, do not stash or reset, and keep documentation and comments
in English.
