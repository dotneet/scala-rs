# Handoff: 2026-09-13

`main` is `b01cbd34` (its gate, `f16daac1`, is `VERDICT=PASS`). Everything below
was produced in one session from `27df6bb3`, through four accepted gates.

## Where the compiler stands

| measure | 2026-09-11 (`c0c10f08`) | now (`main`) |
|---|---:|---:|
| cats (340 files) | 31 errors | **0 errors, 2976 classes** |
| gitbucket (354 inputs) | 92 errors | **0 errors, 1317 classes** |
| scala library (538 files) | 383 errors | **66 errors / 30 files** |
| slick (184 files) | 0 errors / 1504 classes | unchanged, 12/12 execution, 1504 verified |
| corpus pos / neg / run | 1158 / 718 / 707 | **1239 / 815 / 1002** |
| workspace suite | 2802 tests | **3145 tests**, 0 failed |
| `tests/rt_probe.sh` | 71 ok / 13 miscompile / 14 runtime-err / 12 wrong-accept | **114 ok / 3 / 0 / 0** |
| gate wall clock | 52 min (under load) | **14:38** |

Accepted gates, in order: `8ece0269` (twenty slices), `631b238d` (cats and
gitbucket to zero), `f16daac1` (library clusters, a 9x faster typer, a parallel
gate). Each is written up in `tests/BASELINE.md` with its ledger under
`tests/baselines/`. The current ledger is `corpus-f16daac1.tsv`.

## Gate invariants now enforced

`tests/verify_merge.sh` fails unless cats **and** gitbucket compile with zero
errors, slick stays at 0 errors / 1504 classes / 12-of-12 execution / 1504
verified classes, the workspace suite is green with no missing or
signal-killed binaries, and the corpus has `losses=0` — where a loss is
confirmed by a **serial re-run of that one test** first (`run/t5857` asserts its
own wall-clock time, so a loaded machine used to fabricate losses). Per-step
wall times are printed. `GATE_SERIAL=1` restores the old serial behaviour.

## What is NOT verified yet (the honest gap)

Compiling is not running. cats and gitbucket only started reaching code
generation this session, and a five-minute smoke test of the emitted cats found
two defects that no compile measure can see:

1. `java.lang.VerifyError: Operand stack underflow` in
   `cats/instances/ParallelInstances.catsParallelForEitherTNestedParallelValidated`
   when any client runs against our cats classes (the classfile lint passes:
   `lint_classes=2976 lint_problems=0`).
2. scalac cannot use parts of our cats: `Functor[[+A]ValidatedNel[String,A]]` is
   not found and `Kleisli`'s signature does not match, i.e. our `ScalaSignature`
   is incomplete for type-lambda-shaped implicit instances. The same client
   compiles against scalac-built cats.

Repros: `/private/tmp/claude-501/.../scratchpad/catsrun/` (`Simple.scala`,
`Client.scala`). gitbucket has had no runtime check at all yet.

## In flight when this was written

Three slices, each on its own worktree branch, all based on `main`:

* `agent/libzero` — the library's last 66 errors (StreamExtensions 12, HashMap 5,
  PartialFunction 5, Exception 4, TreeSet 4, …). Reaching zero will make the
  library reach codegen for the first time; expect new backend defects there.
* `agent/runharness` — `tests/cats_run.sh` + `tests/gitbucket_run.sh` (the model
  is `tests/slick_run.sh`): compile each target with scala-rs and with scalac,
  compile clients both ways, run under `java -Xverify:all`, diff stdout. Owns
  the two defects above.
* `agent/runfail` — the corpus's 509 remaining `run` failures: whitebox macros
  (26), `cannot expand` (23), TypeTag materialisation (17), the
  `Printers$BooleanFlag` overload (12), output mismatches (71), runtime crashes
  (37).

## Process rules that earned their place this session

* Compose, then run the **full workspace suite on the composed tree** before the
  gate: five cross-slice interactions were invisible to every slice's own suite.
* Assign each known root to exactly **one** slice and say so in the others'
  briefs — three slices fixed `var x: T = _` independently.
* Let the slice that wrote the code **merge the integration branch itself** and
  reconcile duplicate rules; the coordinator merges serially.
* Hard roots: try an Opus slice first, escalate to Fable 5.1 only if it fails
  (the user's revised directive; Fable quota is limited).
* Don't start new slices while a gate runs — it doubles the gate's wall time.
* Java is Temurin 17 (`/usr/bin/java`); the Homebrew `openjdk@*` links are JDK 24
  and silently change `names`/`numt` test data.
