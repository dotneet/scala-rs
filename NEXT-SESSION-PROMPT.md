# Prompt for the next session

Paste the block below as the first message of a new session in
`/Users/shinji/projects/scala-rs`.

---

Scala 2.13 互換の Rust 実装コンパイラ scala-rs を完成させてください。チームで協力し、
難易度に応じて Opus5 と Sonnet5 のサブエージェントにタスクを振り分けて、できるだけ
並列に効率よく開発してください。回答は日本語で。

## 最初に読むもの（この順で）

1. `HANDOFF.md` — 前セッションの引き継ぎ。到達点、未着手の仕事、優先順位、そして
   「測り直さずに信じてはいけないもの」が書いてあります。
2. `tests/BASELINE.md` — `main` の全数値。**基準値は測らずにこれを読んでください。**
   自分の木を測ってこの表と比べる、という使い方をします。この表と食い違ったら、
   分岐元が違うか表が古いかのどちらかなので、止めて調べてください。
3. `.agent-brief.md` — 全サブエージェントが読む常設ブリーフ。前セッションで十数節
   増えていて、どれも事故が起きてから書かれたものです。

## 直ちにやること

`git log --oneline -3` と `tests/BASELINE.md` の commit 欄を突き合わせて、`main` が
表と同じ地点にあることを確認してください。

前セッション終了時、**3 本のスライスがコミット済みのまま検証待ちで止まっています**。
作り直さず、これを拾ってください（`HANDOFF.md` に一覧があります）:

- `agent/implicitmemo` — implicit 探索のメモ化。**gitbucket ~170 件の前提条件**で、
  修正そのものは既に別スライスが用意して「遅すぎる」という理由で入れていません
- `worktree-agent-a44905be6f76c9f6a` — `Null` の消去と分割コンパイルの `IllegalAccessError`
- `agent/catstail3` — cats の平坦な尾

各ブランチのワークツリーで `git merge main` → `cargo test --workspace --release` →
届く範囲の計測 → コーパス、の順に検証し、`tests/BASELINE.md` と比べて何も失って
いなければマージしてください。

## 進め方

サブエージェントを投入するときは **必ず `isolation: "worktree"` を付けてください。**
付け忘れると全員が共有の作業ツリーに入り、互いのブランチを踏み合います（前セッションで
1 回起きました）。

ブリーフには「症状を渡し、件数は渡さない。渡すなら**どちらの種類の数字か**を明示する」
と書いてあります。エラー数の見積もりは収量を予測しますが、コーパスの**テスト数**は
上限にしかならず、実測は 0% から 30% まで散ります。

マージ前の検証はコーディネータが独立に行ってください。前セッションではそれが 3 回
実害を防いでいます（A/B の revert が残ったまま緑を報告、存在しないコミットハッシュ、
再現しない数字）。

## 完成の定義について

「エラー数が減ること」を目的にすると、間違った型で下流を黙らせておくのが最適解に
なってしまいます。前セッションで実際に、握り潰しを外したせいで gitbucket が 1373 →
1736 に増えた局面があり、そこから 912 まで下げました。数字より「その診断は正しい
場所で正しいことを言っているか」を基準にしてください。

同様に、**通ることと正しいことは別**です。前セッションで、型検査も verifier も通り、
例外も出ず、ただ答えが違うという欠陥が繰り返し見つかりました。`tests/verify_all.sh`
と `tests/slick_run.sh`、そして scalac に片側をコンパイルさせる相互運用テストが、
その層に届く唯一の手段です。
